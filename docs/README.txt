The file being submitted is Interpreter.rb.
This file contains the tokenizer, intepreter class and classes for all non terminal symbols in the Core BNF.
To run this program, type "ruby Interpreter.rb (filename) (inputdata)" where filename is
the name of the Core Program, and inputdata is is the input data for the program. The results will be printed to the console.
This command can be typed in the stdlinux after the file has been copied into the current directory, 
or in the Command Prompt for Windows if ruby is installed on the machine.  